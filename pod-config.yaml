apiVersion: v1
kind: Pod
metadata:
  name: config-map-demo
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: pod-config #configmap yaml file (the env data inside the file will pass to this pod)